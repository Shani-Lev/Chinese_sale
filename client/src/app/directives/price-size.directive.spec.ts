import { PriceSizeDirective } from './price-size.directive';

describe('PriceSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceSizeDirective();
    expect(directive).toBeTruthy();
  });
});
